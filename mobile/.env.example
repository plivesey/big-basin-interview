# Leave unset on the iOS simulator -- it shares the host's loopback, so the app
# derives http://localhost:3001 automatically. Set this to a LAN IP when running
# on a physical device.
# EXPO_PUBLIC_BACKEND_URL=http://192.168.1.10:3001
